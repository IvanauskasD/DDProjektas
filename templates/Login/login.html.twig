{% extends 'base.html.twig' %}

{% block body %}
    <label>Register as user</label>
    <a href="{{ path('loginUser') }}">For users</a>
    <label>Register as company</label>
    <a href="{{ path('loginCompany') }}">For companies</a>

{% endblock %}